fastapi>=0.100.0
uvicorn>=0.23.0
python-multipart>=0.0.6
pydantic>=2.0.0
# ifcopenshell is huge, use the appropriate wheel or pip install depending on the platform
# For production, we recommend installing via: pip install ifcopenshell
ifcopenshell
